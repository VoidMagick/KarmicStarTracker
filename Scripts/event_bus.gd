extends Node

# Tell Godot to ignore warnings of unused signals
#warning-ignore:unused_signal

#signal centric_change(heliocentric)
#signal year_change(year)
#signal month_change(month)
#signal day_change(day)
#signal hour_change(hour)
#signal minute_change(minute)
